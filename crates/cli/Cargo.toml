[package]
name = "nokk-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Command-line interface and CDP server binary for the nokk headless browser engine."
repository.workspace = true
keywords = ["headless-browser", "stealth", "scraping", "cli", "puppeteer"]
categories = ["command-line-utilities", "web-programming"]

[features]
default = []
# `cargo build --release --bin nokk --features render` → real off-screen canvas
# rasterization (docs/rendering.md). Off by default; the standard binary stays light.
render = ["nokk/render"]

[[bin]]
name = "nokk"
path = "src/main.rs"

[dependencies]
nokk = { workspace = true }
nokk-net = { workspace = true }
nokk-stealth = { workspace = true }
nokk-cdp = { workspace = true }

tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
clap = { workspace = true }
anyhow = { workspace = true }
serde_json = { workspace = true }
url = { workspace = true }
